CFLAGS = -I../lib -g $(shell ../cflags.sh | cut -d'%' -f1)
LIBS = ../libtcpi.a $(shell ../cflags.sh | cut -d'%' -f2)
CLEANFILES = core core.* *.core *.o temp.* *.a *.so configure config.log config.status auto* config.h.in* *.BAK *~

